tenants:
  default:
    services:
      memory: http://localhost:8010/memory
      support: http://localhost:8010/support
security:
  tools:
    support.workflow:
      allowed_roles:
        - Orchestrator
        - User-Agent
      access_mode: read
      user_approval_required: false
