tenants:
  default:
    services:
      memory: "http://127.0.0.1:8011/mcp/memory"
      crm: "http://127.0.0.1:8000/mcp/crm"
      backend: http://127.0.0.1:8000
security:
  default_allowed_roles:
    - Admin
    - Orchestrator
    - CRM-Supervisor
    - Agent
rate_limits:
  default_per_minute: 120
  per_actor_per_minute: 60
  window_seconds: 60
